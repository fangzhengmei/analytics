# Plausible Analytics 实时访客指标实现分析

## 1. 概述

Plausible Analytics 的实时访客指标采用了一套精心设计的近实时刷新机制，能够在界面上展示接近实时的在线用户数量。本文将从数据采集、存储、查询聚合到前端刷新的完整流程进行深入分析。

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              数据采集层                                    │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Tracker 脚本 → 事件接收 → Pipeline 处理 → WriteBuffer 缓冲      │   │
│  │  (pageview/自定义事件)    (过滤/解析/生成user_id)    (批量写入)   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                              数据存储层                                    │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    ClickHouse (events_v2 表)                      │   │
│  │  ┌─────────────────────────────────────────────────────────┐    │   │
│  │  │ 字段: site_id, timestamp, user_id, name, domain, pathname... ││   │
│  │  └─────────────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                              查询聚合层                                    │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  current_visitors.ex → ClickHouse 查询 → 去重统计 user_id        │   │
│  │  (默认查询过去5分钟内的活跃访客)                                    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                              前端刷新层                                    │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  realtime-update-timer.js → tick 事件 → CurrentVisitors 组件    │   │
│  │  (每30秒触发)              (事件监听)         (API 调用更新 UI)    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

## 3. 页面加载后实时刷新的启动时机

### 3.1 启动时间点

实时刷新的定时器在页面加载的**非常早期**就启动了，具体位置在 `dashboard.tsx`：

```javascript
// dashboard.tsx:1-28
import React, { ReactNode } from 'react'
import { createRoot } from 'react-dom/client'
import 'url-search-params-polyfill'

// ... 其他导入 ...
import * as timer from './dashboard/util/realtime-update-timer'
// ... 其他导入 ...

timer.start()  // ← 模块级代码，在任何组件渲染之前就执行

const container = document.getElementById('stats-react-container')

// ... 后续的 React 渲染逻辑 ...
```

**关键要点**：
1. **执行时机**：`timer.start()` 是模块级代码，在 JavaScript 文件加载并解析时立即执行
2. **早于组件渲染**：定时器启动发生在 React 组件挂载（`root.render(app)`）之前
3. **首次 tick 延迟**：使用 `setInterval`，首次触发在 **30 秒后**，而不是立即

### 3.2 首次数据获取

虽然定时器每 30 秒才触发一次，但 `CurrentVisitors` 组件会在挂载时立即获取数据：

```javascript
// current-visitors.js:32-34
useEffect(() => {
  updateCount()  // 组件挂载时立即执行
}, [dashboardState, updateCount])
```

**完整的初始化流程**：

```
1. 页面加载
    ↓
2. JS 文件解析执行
    ↓
3. timer.start() 启动 (设置 30 秒间隔)
    ↓
4. React 组件树渲染
    ↓
5. CurrentVisitors 组件挂载
    ↓
6. useEffect 触发 → 立即调用 updateCount() → API 请求
    ↓
7. 30 秒后 → 首次 tick 事件 → 再次 API 请求
    ↓
8. 之后每 30 秒循环
```

## 4. 轮询间隔与实时缓存的配合机制

### 4.1 缓存策略设计

Plausible 使用了 `@tanstack/react-query` 进行 API 缓存管理，不同场景有不同的缓存 TTL：

```typescript
// api-client.ts:14-21
import { REALTIME_UPDATE_TIME_MS } from '../util/realtime-update-timer'

// 定义不同场景的缓存过期时间 (毫秒)
export const CACHE_TTL_REALTIME = REALTIME_UPDATE_TIME_MS      // 30 秒 (实时视图)
export const CACHE_TTL_SHORT_ONGOING = 5 * 60 * 1000         // 5 分钟 (包含今天的短期视图)
export const CACHE_TTL_LONG_ONGOING = 60 * 60 * 1000         // 1 小时 (包含今天的长期视图)
export const CACHE_TTL_HISTORICAL = 12 * 60 * 60 * 1000      // 12 小时 (历史数据)
```

### 4.2 TTL 与轮询间隔的配合

**注意**：`CurrentVisitors` 组件（实时访客数字）**不使用 React Query 缓存**，而是直接调用原生 `fetch`：

```javascript
// current-visitors.js:18-22
const updateCount = useCallback(() => {
  api
    .get(`/api/stats/${encodeURIComponent(site.domain)}/current-visitors`)
    .then((res) => setCurrentVisitors(res))
}, [site.domain])
```

查看 `api.ts` 中的 `get` 函数：

```javascript
// api.ts:142-157
export async function get(
  url: string,
  dashboardState?: DashboardState,
  ...extraQueryParams: unknown[]
) {
  const queryString = dashboardState
    ? dashboardStateToSearchParams(dashboardState, [...extraQueryParams])
    : serializeUrlParams(getSharedLinkSearchParams())

  const response = await fetch(queryString ? `${url}?${queryString}` : url, {
    signal: abortController.signal,
    headers: { ...getHeaders(), Accept: 'application/json' }
  })

  return handleApiResponse(response)
}
```

**两种不同的刷新策略对比**：

| 数据类型 | 获取方式 | 缓存机制 | 刷新触发 |
|---------|---------|---------|---------|
| 实时访客数字 (`CurrentVisitors`) | 直接 `fetch` | 无缓存 | tick 事件 (30秒) + 组件挂载 |
| 实时访客图 (`VisitorGraph`) | React Query | `CACHE_TTL_REALTIME` (30秒) | tick 事件 + 缓存过期 |
| 其他统计数据 | React Query | 根据周期不同 TTL | 用户操作 + 缓存过期 |

### 4.3 实时视图 (`realtime` period) 的特殊处理

当用户切换到"实时"时间周期时，缓存策略会调整：

```typescript
// api-client.ts:138-162
export const getStaleTime = (props: DashboardTimeSettings): number => {
  if (
    [DashboardPeriod.realtime, DashboardPeriod.realtime_30m].includes(
      props.period
    )
  ) {
    return CACHE_TTL_REALTIME  // 30 秒
  }

  if (isHistoricalPeriod(props)) {
    return CACHE_TTL_HISTORICAL  // 12 小时
  }

  // ... 其他判断
}
```

**设计意图**：
- 实时视图下，缓存 TTL = 30 秒 = 轮询间隔
- 这确保了每次 tick 事件触发时，缓存刚好过期，数据会重新获取
- 避免了"缓存还在有效期内，但轮询已经触发"的无效请求

## 5. 在线人数统计口径

### 5.1 核心统计逻辑

实时访客的统计口径定义在 `current_visitors.ex`：

```elixir
# current_visitors.ex:6-19
def current_visitors(site, duration \\ Duration.new!(minute: -5)) do
  first_datetime =
    NaiveDateTime.utc_now()
    |> NaiveDateTime.shift(duration)
    |> NaiveDateTime.truncate(:second)
  
  ClickhouseRepo.one(
    from e in "events_v2",
      where: ^Plausible.Sites.site_id_query_filter(site),
      where: e.timestamp >= ^first_datetime,
      where: e.name != "engagement",
      select: uniq(e.user_id)
  )
end
```

### 5.2 统计口径详解

#### 5.2.1 时间窗口

**默认窗口**：过去 **5 分钟** 内的事件
- 可通过 `duration` 参数调整（如 `current_visitors_12h` 使用过去 12 小时）

#### 5.2.2 事件过滤条件

| 条件 | 说明 |
|------|------|
| `site_id_query_filter` | 按站点过滤，确保只统计当前站点的数据 |
| `timestamp >= first_datetime` | 时间窗口过滤，只包含最近 5 分钟 |
| `name != "engagement"` | **排除 engagement 事件** |

#### 5.2.3 为什么排除 "engagement" 事件？

`engagement` 事件是 Plausible 的**心跳/活跃检测事件**：
- 当用户在页面上停留一段时间（通常 10 秒）后，会发送 `engagement` 事件
- 这些事件用于计算"停留时间"等指标
- 但对于"在线人数"统计，如果包含这些事件，会导致：
  - 同一用户多次触发，无法准确判断是否真的"在线"
  - 用户离开页面后，最后一个 `engagement` 事件会导致统计偏高

#### 5.2.4 唯一用户标识 (`uniq(e.user_id)`)

`user_id` 的生成逻辑（参见 3.2 节）：
- 基于：用户代理 + 客户端 IP + 域名 + 根域名 + 盐值
- 使用 `SipHash` 算法生成不可逆的 64 位整数
- 盐值定期轮换，保护用户隐私

**统计口径的准确描述**：
> **"在线人数" = 过去 5 分钟内，发送过非 engagement 事件（如 pageview、自定义事件等）的唯一用户数量**

### 5.3 口径的局限性

#### 5.3.1 可能的高估

- **页面关闭后仍被统计**：用户关闭页面后，其最后一个事件仍会在 5 分钟窗口内
- **多设备/多浏览器**：同一用户在不同设备上会被计为多个用户

#### 5.3.2 可能的低估

- **长时间阅读但无事件**：用户在页面上停留超过 5 分钟但只触发了一次 pageview，之后可能被认为"离线"
- **`engagement` 事件被排除**：如果用户只触发了 `engagement` 事件（在阅读页面），可能不会被计入

#### 5.3.3 设计权衡

5 分钟窗口是一个精心设计的权衡：

| 窗口大小 | 优点 | 缺点 |
|---------|------|------|
| 1 分钟 | 非常"实时" | 用户短暂离开就被排除，波动大 |
| 5 分钟 | 平滑统计，用户体验好 | 可能包含已离开的用户 |
| 10 分钟 | 更稳定 | 实时感下降 |

## 6. 延迟来源深度分析

### 6.1 完整延迟链路

从用户访问页面到实时访客数字更新，完整的延迟链路如下：

```
用户访问页面
    ↓
┌─────────────────────────────────────────────────────────────────────┐
│  1. 采集延迟 (ms 级)                                                  │
│     - Tracker 脚本加载和执行                                          │
│     - 事件数据组装和发送                                              │
└─────────────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────────────┐
│  2. 服务端处理延迟 (ms 级)                                            │
│     - 请求接收和解析                                                  │
│     - Pipeline 多步骤处理 (过滤、解析、生成 user_id 等)               │
│     - 写入 WriteBuffer                                                │
└─────────────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────────────┐
│  3. 写入缓冲延迟 (0-5 秒，可配置)                                     │
│     - 默认配置: CLICKHOUSE_FLUSH_INTERVAL_MS = 5000ms              │
│     - 或缓冲区满: CLICKHOUSE_MAX_BUFFER_SIZE_BYTES = 100000 字节   │
└─────────────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────────────┐
│  4. 数据库可见性延迟 (ms 级)                                          │
│     - ClickHouse 数据插入到 MergeTree 表                              │
│     - 数据变为可查询状态 (通常很快，但取决于表引擎配置)                 │
└─────────────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────────────┐
│  5. 前端轮询延迟 (0-30 秒)                                           │
│     - 取决于 tick 事件何时触发                                         │
│     - 最坏情况：刚刷新完就有新数据，需要等 30 秒                      │
└─────────────────────────────────────────────────────────────────────┘
    ↓
实时访客数字更新
```

### 6.2 各延迟环节详细分析

#### 6.2.1 写入缓冲延迟（最可控的延迟）

这是整个链路中**最主要**的延迟来源，也是可配置的：

```elixir
# runtime.exs:132-146
{ch_flush_interval_ms, ""} =
  config_dir
  |> get_var_from_path_or_env("CLICKHOUSE_FLUSH_INTERVAL_MS", "5000")  # 默认 5 秒
  |> Integer.parse()

{ch_max_buffer_size, ""} =
  config_dir
  |> get_var_from_path_or_env("CLICKHOUSE_MAX_BUFFER_SIZE_BYTES", "100000")  # 默认 100KB
  |> Integer.parse()
```

**写入缓冲的工作机制**：

```elixir
# write_buffer.ex:44-60
@impl true
def handle_cast({:insert, row_binary}, state) do
  state = %{
    state
    | buffer: [state.buffer | row_binary],
      buffer_size: state.buffer_size + IO.iodata_length(row_binary)
  }

  if state.buffer_size >= state.max_buffer_size do
    # 缓冲区满，立即刷新
    Logger.notice("#{state.name} buffer full, flushing to ClickHouse")
    Process.cancel_timer(state.timer)
    do_flush(state)
    new_timer = Process.send_after(self(), :tick, state.flush_interval_ms)
    {:noreply, %{state | buffer: [], timer: new_timer, buffer_size: 0}}
  else
    {:noreply, state}
  end
end
```

**刷新触发条件**：
1. **定时器到时**：每 5 秒（默认）自动刷新
2. **缓冲区满**：达到 100KB（默认）时立即刷新
3. **手动调用**：`flush/1` 函数

**高流量场景下的延迟降低**：
- 当流量大时，缓冲区会更快填满，触发"立即刷新"
- 因此，高流量站点的实时性实际上会**更好**

#### 6.2.2 前端轮询延迟（用户感知的延迟）

这是用户**实际感知**到的延迟：

```javascript
// realtime-update-timer.js:1-8
export const REALTIME_UPDATE_TIME_MS = 30_000  // 30 秒
const tickEvent = new Event('tick')

export function start() {
  setInterval(() => {
    document.dispatchEvent(tickEvent)
  }, REALTIME_UPDATE_TIME_MS)
}
```

**轮询延迟的数学期望**：
- 最坏情况：30 秒（刚 tick 完就有新数据）
- 最好情况：接近 0 秒（数据刚写入就 tick）
- **平均期望**：15 秒

**为什么选择 30 秒？**

| 考虑因素 | 分析 |
|---------|------|
| 服务器负载 | 每 30 秒一次 API 调用，对于大量打开的 dashboard 来说，负载可控 |
| 用户体验 | 30 秒的更新频率对于"实时访客"来说，用户感知上已经足够"实时" |
| 与后端缓冲配合 | 后端默认 5 秒缓冲，30 秒轮询确保数据已经写入至少 5 秒，不会查询到空结果 |
| 与缓存 TTL 配合 | `CACHE_TTL_REALTIME = 30_000`，确保每次轮询时缓存已过期 |

#### 6.2.3 数据库可见性延迟

ClickHouse 的 `MergeTree` 引擎特性：
- 数据写入后，通常需要经历一个 `merge` 过程才会完全可见
- 但对于实时查询，ClickHouse 提供了近实时的可见性
- 现代 ClickHouse 版本中，这个延迟通常在 **100ms 以内**

#### 6.2.4 典型场景的延迟估算

**场景 1：低流量站点（如个人博客）**
- 写入缓冲延迟：接近 5 秒（缓冲区不满，等待定时刷新）
- 前端轮询延迟：平均 15 秒
- **总感知延迟**：约 20 秒

**场景 2：高流量站点（如电商首页）**
- 写入缓冲延迟：可能 < 1 秒（缓冲区快速填满）
- 前端轮询延迟：平均 15 秒
- **总感知延迟**：约 16 秒

**场景 3：实时仪表盘用户（刚打开页面）**
- 组件挂载时立即调用 API
- 查询的是过去 5 分钟的数据
- **即时可见**：已有的数据立即显示
- **延迟**：新数据需要等待下一次轮询

### 6.3 延迟优化的可能性

如果需要更低的延迟，理论上可以调整以下参数：

| 参数 | 默认值 | 可调小到 | 风险/代价 |
|------|--------|---------|----------|
| `CLICKHOUSE_FLUSH_INTERVAL_MS` | 5000ms | 1000ms | 数据库写入压力增加 |
| `CLICKHOUSE_MAX_BUFFER_SIZE_BYTES` | 100KB | 10KB | 数据库写入压力增加 |
| `REALTIME_UPDATE_TIME_MS` | 30000ms | 10000ms | 前端 API 调用频率增加 3 倍 |

**注意**：Plausible 目前的配置是经过生产验证的最佳实践，不建议随意调整，除非确实有强烈的低延迟需求。

## 7. 数据采集与处理流程

### 7.1 事件接收与处理 Pipeline

事件的处理核心位于 `lib/plausible/ingestion/event.ex`，采用了 Pipeline 模式对事件进行多步骤处理：

```elixir
# 核心处理流程 pipeline (event.ex:130-151)
defp pipeline() do
  [
    drop_verification_agent: &drop_verification_agent/2,   # 过滤验证代理
    drop_datacenter_ip: &drop_datacenter_ip/2,               # 过滤数据中心 IP
    drop_threat_ip: &drop_threat_ip/2,                       # 过滤威胁 IP
    drop_shield_rule_hostname: &drop_shield_rule_hostname/2, # 主机名过滤
    drop_shield_rule_page: &drop_shield_rule_page/2,         # 页面规则过滤
    drop_shield_rule_ip: &drop_shield_rule_ip/2,             # IP 规则过滤
    put_geolocation: &put_geolocation/2,                      # 地理位置解析
    drop_shield_rule_country: &drop_shield_rule_country/2,   # 国家规则过滤
    put_user_agent: &put_user_agent/2,                        # 用户代理解析
    put_basic_info: &put_basic_info/2,                        # 基础信息填充
    put_source_info: &put_source_info/2,                      # 来源信息解析
    maybe_infer_medium: &maybe_infer_medium/2,                # 推断媒介
    put_props: &put_props/2,                                   # 自定义属性
    put_revenue: &put_revenue/2,                               # 收入数据
    put_salts: &put_salts/2,                                   # 获取盐值
    put_user_id: &put_user_id/2,                               # 生成用户 ID
    validate_clickhouse_event: &validate_clickhouse_event/2,  # 验证事件
    register_session: &register_session/2                      # 注册会话
  ]
end
```

### 7.2 用户 ID 生成机制

用户 ID 是实时访客统计的核心标识，生成逻辑如下：

```elixir
# event.ex:553-567
defp generate_user_id(request, domain, hostname, salt) do
  cond do
    is_nil(salt) -> nil
    is_nil(domain) -> nil
    true ->
      user_agent = request.user_agent || ""
      root_domain = get_root_domain(hostname)
      
      # 使用 SipHash 算法生成唯一用户 ID
      SipHash.hash!(salt, user_agent <> request.remote_ip <> domain <> root_domain)
  end
end
```

**关键要点**：
- 使用 `SipHash` 算法生成不可逆的用户标识
- 输入因子包括：用户代理、客户端 IP、域名、根域名
- 结合盐值（定期轮换）增强安全性和隐私保护

### 7.3 写入缓冲机制

为了提高写入性能，Plausible 使用了写入缓冲机制：

```elixir
# write_buffer.ex:20-41
@impl true
def init(opts) do
  buffer = opts[:buffer] || []
  max_buffer_size = opts[:max_buffer_size] || default_max_buffer_size()
  flush_interval_ms = opts[:flush_interval_ms] || default_flush_interval_ms()
  
  Process.flag(:trap_exit, true)
  timer = Process.send_after(self(), :tick, flush_interval_ms)
  
  {:ok,
   %{
     buffer: buffer,
     timer: timer,
     name: Keyword.fetch!(opts, :name),
     insert_sql: Keyword.fetch!(opts, :insert_sql),
     insert_opts: Keyword.fetch!(opts, :insert_opts),
     header: Keyword.fetch!(opts, :header),
     buffer_size: IO.iodata_length(buffer),
     max_buffer_size: max_buffer_size,
     flush_interval_ms: flush_interval_ms
   }}
end
```

**刷新触发条件**：
1. **定时刷新**：通过 `Process.send_after` 设置定时器，默认 5 秒
2. **缓冲区满**：当 `buffer_size >= max_buffer_size`（默认 100KB）时立即刷新
3. **手动刷新**：通过 `flush/1` 函数手动触发

## 8. 实时数据查询与聚合

### 8.1 current_visitors 核心查询

实时访客统计的核心查询位于 `lib/plausible/stats/current_visitors.ex`：

```elixir
# current_visitors.ex:1-19
defmodule Plausible.Stats.CurrentVisitors do
  use Plausible.ClickhouseRepo
  use Plausible.Stats.SQL.Fragments
  
  @spec current_visitors(Plausible.Site.t(), Duration.duration()) :: non_neg_integer
  def current_visitors(site, duration \\ Duration.new!(minute: -5)) do
    first_datetime =
      NaiveDateTime.utc_now()
      |> NaiveDateTime.shift(duration)
      |> NaiveDateTime.truncate(:second)
    
    ClickhouseRepo.one(
      from e in "events_v2",
        where: ^Plausible.Sites.site_id_query_filter(site),
        where: e.timestamp >= ^first_datetime,
        where: e.name != "engagement",
        select: uniq(e.user_id)
    )
  end
end
```

**查询要点**：
1. **时间窗口**：默认查询过去 **5 分钟** 内的事件（可通过 `duration` 参数调整）
2. **数据过滤**：
   - 按站点过滤：`site_id_query_filter`
   - 按时间过滤：`timestamp >= first_datetime`
   - 排除事件：`name != "engagement"`（排除心跳/活跃事件）
3. **聚合方式**：`uniq(e.user_id)` - 统计唯一用户 ID 的数量

### 8.2 API 接口层

API 控制器提供了对外查询接口：

```elixir
# stats_controller.ex:1236-1239
def current_visitors(conn, _) do
  site = conn.assigns[:site]
  json(conn, Stats.current_visitors(site))
end
```

**注意**：
- 该接口不受 `validate_required_filters_plug` 过滤限制（第 38 行）
- 直接返回数字类型的访客数量

## 9. 前端实时刷新机制

### 9.1 定时触发器

前端刷新的核心是 `realtime-update-timer.js`：

```javascript
// realtime-update-timer.js:1-8
export const REALTIME_UPDATE_TIME_MS = 30_000  // 30 秒
const tickEvent = new Event('tick')

export function start() {
  setInterval(() => {
    document.dispatchEvent(tickEvent)
  }, REALTIME_UPDATE_TIME_MS)
}
```

**刷新频率**：每 **30 秒** 触发一次 `tick` 事件

### 9.2 事件监听与数据更新

`CurrentVisitors` 组件监听 `tick` 事件并更新数据：

```javascript
// current-visitors.js:12-34
export default function CurrentVisitors({ className = '' }) {
  const { dashboardState } = useDashboardStateContext()
  const lastLoadTimestamp = useLastLoadContext()
  const site = useSiteContext()
  const [currentVisitors, setCurrentVisitors] = useState(null)
  
  // 定义更新函数
  const updateCount = useCallback(() => {
    api
      .get(`/api/stats/${encodeURIComponent(site.domain)}/current-visitors`)
      .then((res) => setCurrentVisitors(res))
  }, [site.domain])
  
  // 监听 tick 事件
  useEffect(() => {
    document.addEventListener('tick', updateCount)
    
    return () => {
      document.removeEventListener('tick', updateCount)
    }
  }, [updateCount])
  
  // 初始加载和筛选器变化时触发
  useEffect(() => {
    updateCount()
  }, [dashboardState, updateCount])
  
  // ... 渲染逻辑
}
```

**更新触发条件**：
1. **定时触发**：每 30 秒的 `tick` 事件
2. **状态变化**：`dashboardState` 变化时（如筛选器调整）
3. **组件挂载**：首次加载时

### 9.3 tick 事件的其他消费者

除了 `CurrentVisitors` 组件，还有其他组件也监听 `tick` 事件：

| 组件/文件 | 用途 |
|-----------|------|
| `last-load-context.tsx` | 更新最后加载时间戳 |
| `visitor-graph.tsx` | 实时访客图更新 |
| `reports/list.tsx` | 报告列表更新 |
| `locations/map.tsx` | 地图数据更新 |

## 10. 代码位置速查

| 功能模块 | 文件路径 | 关键行号 |
|---------|---------|---------|
| 事件处理 Pipeline | `lib/plausible/ingestion/event.ex` | 130-151 |
| 用户 ID 生成 | `lib/plausible/ingestion/event.ex` | 553-567 |
| 写入缓冲机制 | `lib/plausible/ingestion/write_buffer.ex` | 20-110 |
| 实时访客查询 | `lib/plausible/stats/current_visitors.ex` | 1-19 |
| API 接口 | `lib/plausible_web/controllers/api/stats_controller.ex` | 1236-1239 |
| 前端定时器启动 | `assets/js/dashboard.tsx` | 28 |
| 定时器实现 | `assets/js/dashboard/util/realtime-update-timer.js` | 1-8 |
| 缓存 TTL 配置 | `assets/js/dashboard/hooks/api-client.ts` | 14-21 |
| 实时访客组件 | `assets/js/dashboard/stats/current-visitors.js` | 12-34 |
| 刷新间隔配置 | `config/runtime.exs` | 132-146 |

## 11. 总结

### 11.1 核心设计要点

#### 数据层面
- **基于时间窗口的统计**：默认查询过去 5 分钟内的事件
- **唯一用户标识**：使用 SipHash 算法生成匿名化的 user_id
- **ClickHouse 高效聚合**：利用列式存储和 `uniq()` 函数实现快速去重统计
- **排除 engagement 事件**：确保在线人数统计的准确性

#### 性能优化
- **写入缓冲**：通过 WriteBuffer 实现批量写入，默认 5 秒刷新
- **事件过滤**：Pipeline 模式提前过滤无效事件和垃圾流量
- **盐值轮换**：保护用户隐私的同时保持统计一致性

#### 前端刷新
- **启动时机**：JS 模块加载时立即启动定时器
- **首次获取**：组件挂载时立即 API 调用，不依赖首次 tick
- **事件驱动**：基于 DOM 事件 (`tick`) 的解耦设计
- **缓存配合**：实时视图下缓存 TTL = 轮询间隔 = 30 秒

#### 延迟控制
- **写入缓冲延迟**：0-5 秒（可配置）
- **前端轮询延迟**：0-30 秒（平均 15 秒）
- **高流量场景**：缓冲区快速填满，实际延迟更低

### 11.2 设计权衡

| 设计决策 | 优点 | 缺点 |
|---------|------|------|
| 5 分钟时间窗口 | 平滑统计，用户体验好 | 可能包含已离开的用户 |
| 30 秒轮询间隔 | 服务器负载可控，用户体验可接受 | 新数据最多等待 30 秒 |
| 5 秒写入缓冲 | 减少数据库压力 | 数据可见性延迟 |
| 排除 engagement 事件 | 避免重复统计 | 某些场景可能低估 |

这种设计使得 Plausible 能够在保证数据准确性的同时，提供接近实时的访客统计体验，同时保持了良好的系统扩展性和性能表现。
