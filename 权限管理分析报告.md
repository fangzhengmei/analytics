# Plausible Analytics 站点访问权限管理分析报告

## 一、权限层级定义

### 1.1 角色类型定义

Plausible Analytics 的权限系统基于**团队成员关系**和**访客成员关系**两种模型，定义了以下角色层级：

| 角色 | 权限级别 | 适用范围 | 说明 |
|------|----------|----------|------|
| `:owner` | 最高 | 团队/站点 | 站点所有者，拥有全部权限，可转让所有权 |
| `:admin` | 高 | 团队/站点 | 管理员，可管理用户、修改设置 |
| `:editor` | 中 | 团队/站点/访客 | 编辑者，可查看统计并编辑站点设置 |
| `:viewer` | 低 | 团队/站点/访客 | 查看者，仅可查看统计数据 |
| `:guest` | 特殊 | 团队 | 访客标记，需配合 GuestMembership 使用 |
| `:billing` | 特殊 | 团队 | 账单角色，用于企业版订阅管理 |
| `:super_admin` | 系统 | 全局 | 系统超级管理员（非数据库字段） |
| `:public` | 公开 | 站点 | 公开访问角色（用于公开站点和共享链接） |

**角色定义源代码位置**：`lib/plausible/teams/membership.ex:10`

```elixir
@roles [:guest, :viewer, :editor, :admin, :owner, :billing]
```

### 1.2 成员类型区分

系统通过两种成员关系实现灵活的权限控制：

#### 1.2.1 团队成员关系 (Team Membership)

- **表名**：`team_memberships`
- **模型**：`Plausible.Teams.Membership`
- **特点**：用户属于整个团队，对团队内所有站点拥有权限

**数据结构**：
```elixir
schema "team_memberships" do
  field :role, Ecto.Enum, values: @roles  # [:guest, :viewer, :editor, :admin, :owner, :billing]
  field :is_autocreated, :boolean, default: false
  
  belongs_to :user, Plausible.Auth.User
  belongs_to :team, Plausible.Teams.Team
  has_many :guest_memberships, Plausible.Teams.GuestMembership
end
```

#### 1.2.2 访客成员关系 (Guest Membership)

- **表名**：`guest_memberships`
- **模型**：`Plausible.Teams.GuestMembership`
- **特点**：用户仅对特定站点有权限，属于细粒度授权

**数据结构**：
```elixir
schema "guest_memberships" do
  field :role, Ecto.Enum, values: [:viewer, :editor]  # 访客角色仅限于这两种
  
  belongs_to :team_membership, Plausible.Teams.Membership
  belongs_to :site, Plausible.Site
end
```

**关键设计**：
1. 访客成员必须先有一个 `team_membership` 记录，其 `role` 为 `:guest`
2. 实际权限由 `guest_memberships` 表中的 `role` 字段决定（`:viewer` 或 `:editor`）
3. 一个访客可以被授权访问团队内的多个站点

### 1.3 角色权限矩阵

| 操作 | Owner | Admin | Editor | Viewer | Guest (需站点授权) |
|------|-------|-------|--------|--------|---------------------|
| 查看统计数据 | ✅ | ✅ | ✅ | ✅ | ✅ (需对应站点) |
| 编辑站点设置 | ✅ | ✅ | ✅ | ❌ | ✅ (若为 editor) |
| 邀请/管理成员 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 修改他人角色 | ✅ | ✅ (不能改 Owner) | ❌ | ❌ | ❌ |
| 转让所有权 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 删除站点 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 创建共享链接 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 管理团队设置 | ✅ | ✅ | ❌ | ❌ | ❌ |

---

## 二、共享报表访问范围控制

### 2.1 共享链接机制

**模型位置**：`lib/plausible/site/shared_link.ex`

Plausible 通过 `SharedLink` 模型实现报表的受控共享：

```elixir
schema "shared_links" do
  belongs_to :site, Plausible.Site
  field :name, :string
  field :slug, :string           # URL 路径中的唯一标识
  field :password_hash, :string  # 密码保护（可选）
  field :password, :string, virtual: true
  belongs_to :segment, Plausible.Segments.Segment  # 数据片段限制（可选）
end
```

### 2.2 共享链接的访问控制维度

#### 2.2.1 公开访问 vs 私有访问

**公开站点**：
- 站点设置 `public: true`
- 任何访问者都以 `:public` 角色查看统计
- **代码位置**：`lib/plausible_web/plugs/authorize_site_access.ex:94-95`

```elixir
site.public -> :public
```

#### 2.2.2 密码保护

共享链接可设置密码保护：
```elixir
def password_protected?(%__MODULE__{password_hash: hash}) when not is_nil(hash), do: true
def password_protected?(%__MODULE__{}), do: false
```

**验证流程**：
1. 用户访问 `/share/:domain/:slug` 路径
2. 系统检查 `shared_links` 表是否有对应的 `slug`
3. 若有密码保护，需验证密码
4. 验证通过后授予 `:public` 角色访问权限

**代码位置**：`lib/plausible_web/plugs/authorize_site_access.ex:200-220`

#### 2.2.3 数据片段限制

共享链接可绑定到特定的数据片段（Segment）：
```elixir
def limited_to_segment?(%__MODULE__{segment_id: segment_id}) when is_integer(segment_id), do: true
def limited_to_segment?(%__MODULE__{}), do: false
```

**作用**：
- 限制共享报表仅显示符合片段条件的数据
- 例如：只分享某个页面的数据，或某个国家的访客数据

### 2.3 共享链接路由

**路由定义位置**：`lib/plausible_web/router.ex:487-491`

```elixir
scope "/", PlausibleWeb do
  pipe_through [:shared_link]
  
  get "/share/:domain/*path", StatsController, :shared_link
  post "/share/:slug/authenticate", StatsController, :authenticate_shared_link
end
```

**`shared_link` pipeline 特点**：
- 不经过 `AuthPlug`（无需登录）
- 使用独立的安全头配置 `SecureEmbedHeaders`
- 支持 iframe 嵌入

---

## 三、接口层权限校验实现

### 3.1 核心授权中间件

系统通过两个主要 Plug 实现接口层的权限校验：

#### 3.1.1 AuthorizeSiteAccess

**位置**：`lib/plausible_web/plugs/authorize_site_access.ex`

**功能**：验证用户对特定站点的访问权限

**初始化配置**：
```elixir
# 允许所有角色访问
plug AuthorizeSiteAccess

# 或
plug AuthorizeSiteAccess, :all_roles

# 仅允许特定角色
plug AuthorizeSiteAccess, [:admin, :owner, :super_admin]

# 使用自定义参数名获取站点
plug AuthorizeSiteAccess, {[:admin, :owner], "site_id"}
```

**权限检查流程**（`call/2` 函数）：

1. **获取站点域名**：从 URL 参数或请求参数中提取
2. **获取用户角色**：调用 `Teams.Memberships.site_role/2`
3. **检查访问类型**：
   - 团队成员：`{:team_member, role}`
   - 访客成员：`{:guest_member, role}`
   - 非成员：`{:error, :not_a_member}`
4. **备选访问方式检查**：
   - 超级管理员：`is_super_admin?(current_user)` → `:super_admin`
   - 公开站点：`site.public` → `:public`
   - 共享链接：存在有效共享链接 → `:public`
5. **角色验证**：检查解析出的角色是否在允许列表中
6. **设置连接上下文**：
   - `assigns.site`：站点对象
   - `assigns.site_role`：用户角色
   - `assigns.shared_link`：共享链接对象（若有）
   - `assigns.site_team`：站点所属团队

**关键代码片段**：
```elixir
role =
  cond do
    membership_role -> membership_role
    Plausible.Auth.is_super_admin?(current_user) -> :super_admin
    site.public -> :public
    shared_link -> :public
    true -> nil
  end

if role in allowed_roles do
  # 授权通过，设置上下文
else
  error_not_found(conn)
end
```

#### 3.1.2 AuthorizeTeamAccess

**位置**：`lib/plausible_web/plugs/authorize_team_access.ex`

**功能**：验证用户对特定团队的访问权限

**特点**：
- 依赖 `:current_team_role`（由 `AuthPlug` 预先填充）
- 不包含 `:guest` 角色（访客不属于团队级成员）
- 若没有当前团队，则放行（用于显示通知等场景）

**代码**：
```elixir
@all_roles Plausible.Teams.Membership.roles() -- [:guest]

def call(conn, roles \\ @all_roles) do
  current_team = conn.assigns[:current_team]
  
  if current_team do
    current_team_role = conn.assigns[:current_team_role]
    
    if current_team_role in roles do
      conn
    else
      conn
      |> Phoenix.Controller.redirect(to: Routes.site_path(conn, :index))
      |> halt()
    end
  else
    conn  # 没有团队则放行
  end
end
```

### 3.2 路由中的权限配置示例

#### 3.2.1 内部统计 API（最宽松）

```elixir
pipeline :internal_stats_api do
  plug :accepts, ["json"]
  plug :fetch_session
  plug PlausibleWeb.AuthPlug
  plug PlausibleWeb.Plugs.AuthorizeSiteAccess  # 所有角色均可访问
  plug PlausibleWeb.Plugs.NoRobots
end
```

#### 3.2.2 文档查询 API（较严格）

```elixir
pipeline :docs_stats_api do
  plug :accepts, ["json"]
  plug :fetch_session
  plug PlausibleWeb.AuthPlug
  
  plug PlausibleWeb.Plugs.AuthorizeSiteAccess,
       {[:admin, :editor, :super_admin, :owner], "site_id"}
  # 仅允许 admin、editor、super_admin、owner
end
```

#### 3.2.3 成员管理控制器（最严格）

**位置**：`lib/plausible_web/controllers/site/membership_controller.ex:20`

```elixir
plug PlausibleWeb.Plugs.AuthorizeSiteAccess, [:owner, :admin]
# 仅 Owner 和 Admin 可访问成员管理
```

### 3.3 站点角色查询逻辑

**位置**：`lib/plausible/teams/memberships.ex:80-101`

```elixir
@spec site_role(Plausible.Site.t(), Auth.User.t() | nil) ::
        {:ok, {:team_member | :guest_member, Teams.Membership.role()}} | {:error, :not_a_member}

def site_role(site, user) do
  result =
    from(u in Auth.User,
      inner_join: tm in assoc(u, :team_memberships),
      left_join: gm in assoc(tm, :guest_memberships),
      where: tm.team_id == ^site.team_id and tm.user_id == ^user.id,
      where: tm.role != :guest or gm.site_id == ^site.id,
      select: {tm.role, gm.role}
    )
    |> Repo.one()

  case result do
    {:guest, role} -> {:ok, {:guest_member, role}}  # 访客，使用 guest_membership 的角色
    {role, _} -> {:ok, {:team_member, role}}         # 团队成员，直接使用 team_membership 的角色
    _ -> {:error, :not_a_member}
  end
end
```

**查询逻辑说明**：
1. 关联 `team_memberships` 表获取用户在团队中的角色
2. 左连接 `guest_memberships` 表检查是否有特定站点的访客权限
3. **关键条件**：`tm.role != :guest or gm.site_id == ^site.id`
   - 若用户不是 `:guest` 角色，则直接有权限
   - 若用户是 `:guest` 角色，则必须有对应站点的 `guest_membership` 记录
4. 返回结果区分 `:team_member` 和 `:guest_member` 两种类型

---

## 四、前端展示层权限控制

### 4.1 服务端模板渲染控制

#### 4.1.1 基于 `@site_role` 的条件渲染

**位置**：`lib/plausible_web/templates/site/settings_people.html.heex`

**示例1：邀请按钮的显示控制**
```heex
<.filter_bar :if={@site_role in [:owner, :admin]} filtering_enabled?={false}>
  <.button_link
    mt?={false}
    href={Routes.membership_path(@conn, :invite_member_form, @site.domain)}
  >
    Invite new guest
  </.button_link>
</.filter_bar>
```

**示例2：角色下拉菜单的权限控制**
```heex
<.dropdown_item
  href={Routes.membership_path(...)}
  method="put"
  disabled={@site_role not in [:owner, :admin] or membership.role == "editor"}
>
  <div>Guest editor</div>
</.dropdown_item>

<.dropdown_item
  href={Routes.membership_path(...)}
  class="!text-red-600"
  method="delete"
  disabled={@site_role not in [:owner, :admin]}
>
  Remove member
</.dropdown_item>
```

**示例3：团队设置通知的显示**
```heex
<PlausibleWeb.Team.Notice.owner_cta_banner :if={@site_role == :owner} />
<PlausibleWeb.Team.Notice.guest_cta_banner :if={@site_role != :owner} />
```

#### 4.1.2 统计页面的数据注入

**位置**：`lib/plausible_web/templates/stats/stats.html.heex`

用户角色通过 `data-*` 属性传递给前端 React 应用：

```heex
<div
  id="stats-react-container"
  data-domain={@site.domain}
  data-current-user-role={@site_role}  <!-- 用户角色 -->
  data-current-user-id={
    if user = @conn.assigns[:current_user], do: user.id, else: Jason.encode!(nil)
  }
  data-logged-in={to_string(!!@conn.assigns[:current_user])}
  data-shared-link-auth={assigns[:shared_link_auth]}
  ...
>
</div>
```

### 4.2 前端组件中的权限判断

#### 4.2.1 React 组件使用 `data-current-user-role`

前端 React 应用从 DOM 元素的 `data-*` 属性中读取用户角色：

```javascript
// 伪代码示例
const container = document.getElementById('stats-react-container');
const userRole = container.dataset.currentUserRole;

// 根据角色控制 UI
const canEdit = ['owner', 'admin', 'editor'].includes(userRole);
const canManageUsers = ['owner', 'admin'].includes(userRole);
```

#### 4.2.2 团队锁定状态的显示

**位置**：`lib/plausible_web/templates/stats/stats.html.heex:4-12`

```heex
<%= if Plausible.Teams.locked?(@site.team) do %>
  <div
    class="w-full px-4 py-4 text-sm font-bold text-center text-yellow-800 bg-yellow-100..."
    role="alert"
  >
    <p>This dashboard is actually locked. You are viewing it with super-admin access</p>
  </div>
<% end %>
```

### 4.3 共享链接设置的 LiveView

**位置**：`lib/plausible_web/live/shared_link_settings.ex`（通过 LiveComponent 渲染）

在 `settings_visibility.html.heex` 中：
```heex
<div :if={Plausible.Billing.Feature.SharedLinks.enabled?(@site)}>
  {live_render(@conn, PlausibleWeb.Live.SharedLinkSettings,
    session: %{"site_id" => @site.id, "domain" => @site.domain}
  )}
</div>
```

**权限检查**：
- 访问此页面需要先通过 `AuthorizeSiteAccess` 中间件
- 实际的共享链接管理操作在 LiveView 内部可能还有额外的权限校验

---

## 五、权限授予与变更机制

### 5.1 角色授予规则

**位置**：`lib/plausible/teams/memberships.ex:179-183`

```elixir
defp can_grant_role_to_other?(:owner, :editor), do: true
defp can_grant_role_to_other?(:owner, :viewer), do: true
defp can_grant_role_to_other?(:admin, :editor), do: true
defp can_grant_role_to_other?(:admin, :viewer), do: true
defp can_grant_role_to_other?(_, _), do: false
```

**规则说明**：
| 执行者 | 可授予的角色 | 不可授予的角色 |
|--------|--------------|----------------|
| Owner | editor, viewer | owner, admin, billing, guest |
| Admin | editor, viewer | owner, admin, billing, guest |
| Editor | 无 | 所有 |
| Viewer | 无 | 所有 |

### 5.2 角色更新逻辑

**位置**：`lib/plausible/teams/memberships.ex:130-157`

```elixir
def update_role(site, user_id, new_role_str, current_user, current_user_role) do
  new_role = String.to_existing_atom(new_role_str)
  
  case get_guest_membership(site.id, user_id) do
    {:ok, guest_membership} ->
      can_grant_role? =
        if guest_membership.team_membership.user_id == current_user.id do
          false  # 不能修改自己的角色
        else
          can_grant_role_to_other?(current_user_role, new_role)
        end
      
      if can_grant_role? do
        # 更新角色
      else
        {:error, :not_allowed}
      end
    
    {:error, _} ->
      {:error, :no_guest}
  end
end
```

**关键约束**：
1. **不能修改自己的角色**：若目标用户是当前用户，直接拒绝
2. **遵循授予规则**：调用 `can_grant_role_to_other?/2` 验证
3. **仅针对访客**：此函数仅处理 `guest_membership`（团队成员角色变更有单独逻辑）

### 5.3 所有权转让

**位置**：`lib/plausible_web/controllers/site/membership_controller.ex:110-142`

所有权转让通过邀请机制实现：
1. Owner 发起转让邀请（目标角色为 `:owner`）
2. 被邀请者接受邀请后成为新的 Owner
3. 原 Owner 可能降级或保持其他角色（取决于具体实现）

### 5.4 添加站点权限判断

**位置**：`lib/plausible/teams/memberships.ex:63-78`

```elixir
@spec can_add_site?(Teams.Team.t(), Auth.User.t()) :: boolean()
def can_add_site?(team, user) do
  user_type = Plausible.Users.type(user)
  
  role =
    case team_role(team, user) do
      {:ok, role} -> role
      {:error, _} -> :not_a_member
    end
  
  case {user_type, role, team} do
    {:sso, :owner, %{setup_complete: false}} -> false  # SSO 用户在团队未完成设置时不能添加站点
    {_, role, _} when role in [:owner, :admin, :editor] -> true  # 这三种角色可以添加站点
    _ -> false
  end
end
```

---

## 六、架构总结

### 6.1 权限体系架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         访问请求入口                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
    ┌──────────┐   ┌──────────┐   ┌──────────────────┐
    │ AuthPlug │   │ Shared   │   │ Public API (Key) │
    │ (登录认证)│   │ Link     │   │ (API 密钥认证)   │
    └────┬─────┘   │ Pipeline │   └────────┬─────────┘
         │          └────┬─────┘            │
         │               │                  │
         ▼               ▼                  ▼
    ┌─────────────────────────────────────────────────────┐
    │              AuthorizeSiteAccess Plug                │
    │  (站点级权限校验 - 核心授权层)                        │
    ├─────────────────────────────────────────────────────┤
    │  权限来源优先级（从高到低）：                          │
    │  1. 团队成员角色 (team_memberships.role)             │
    │  2. 访客成员角色 (guest_memberships.role)            │
    │  3. 超级管理员 (is_super_admin?)                     │
    │  4. 公开站点 (site.public)                           │
    │  5. 有效共享链接 (shared_link)                        │
    └───────────────────────────┬─────────────────────────┘
                                │
                    ┌───────────┼───────────┐
                    ▼           ▼           ▼
              ┌─────────┐ ┌─────────┐ ┌─────────┐
              │ :owner  │ │ :admin  │ │ :editor │
              │ :viewer │ │ :guest  │ │ :public │
              └────┬────┘ └────┬────┘ └────┬────┘
                   │            │            │
                   ▼            ▼            ▼
    ┌─────────────────────────────────────────────────────┐
    │                 前端展示层控制                        │
    ├─────────────────────────────────────────────────────┤
    │  服务端模板：@site_role 条件渲染                      │
    │  React 组件：data-current-user-role 属性             │
    │  LiveView：基于角色的功能按钮可见性                   │
    └─────────────────────────────────────────────────────┘
```

### 6.2 关键设计亮点

1. **双层成员关系**：
   - 团队成员：粗粒度，适用于团队协作
   - 访客成员：细粒度，适用于外部合作方

2. **多渠道授权**：
   - 用户登录认证
   - 共享链接（支持密码保护）
   - 公开站点
   - API 密钥

3. **职责分离**：
   - `AuthorizeSiteAccess`：站点级权限（最常用）
   - `AuthorizeTeamAccess`：团队级权限（管理场景）

4. **前端控制**：
   - 服务端渲染时通过 `@site_role` 控制模板渲染
   - 前端应用通过 `data-*` 属性获取角色信息
   - 双重保障：接口层校验 + 展示层控制

### 6.3 数据流示例：用户访问统计页面

```
1. 用户请求 GET /example.com/stats
        │
        ▼
2. Router 匹配到 :internal_stats_api pipeline
        │
        ▼
3. AuthPlug 验证登录，设置 :current_user
        │
        ▼
4. AuthorizeSiteAccess 执行：
   ├─► 从 URL 获取 domain = "example.com"
   ├─► 查询 Site 记录
   ├─► 调用 Teams.Memberships.site_role(site, current_user)
   ├─► 检查是否为团队成员或访客成员
   ├─► 若不是成员，检查是否为公开站点或有共享链接
   └─► 验证通过，设置 assigns.site_role = :viewer (示例)
        │
        ▼
5. StatsController 处理请求，准备数据
        │
        ▼
6. 模板渲染：
   ├─► 检查 @site_role 决定显示哪些按钮
   └─► 注入 data-current-user-role="viewer" 到 DOM
        │
        ▼
7. 前端 React 应用初始化：
   └─► 读取 data-current-user-role，控制 UI 交互
```

---

## 七、参考文件清单

| 文件路径 | 说明 |
|----------|------|
| `lib/plausible/teams/membership.ex` | 团队成员模型与角色定义 |
| `lib/plausible/teams/guest_membership.ex` | 访客成员模型 |
| `lib/plausible/teams/memberships.ex` | 成员关系查询与权限判断 |
| `lib/plausible/teams/policy.ex` | 团队策略（SSO、2FA 等） |
| `lib/plausible/site/shared_link.ex` | 共享链接模型 |
| `lib/plausible_web/plugs/authorize_site_access.ex` | 站点授权中间件（核心） |
| `lib/plausible_web/plugs/authorize_team_access.ex` | 团队授权中间件 |
| `lib/plausible_web/controllers/site/membership_controller.ex` | 成员管理控制器 |
| `lib/plausible_web/router.ex` | 路由与 pipeline 配置 |
| `lib/plausible_web/templates/site/settings_people.html.heex` | 人员管理模板（前端权限示例） |
| `lib/plausible_web/templates/stats/stats.html.heex` | 统计页面模板（角色数据注入） |

---

*报告生成时间：2026-05-02*
*基于代码库版本：g:/fangzheng/solo-dogfeeding/code/17716-analytics*
